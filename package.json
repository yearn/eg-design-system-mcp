{
  "name": "eg",
  "private": true,
  "scripts": {
    "dev": "(cd packages/react && bun dev)",
    "build": "bun run build:react && bun run build:mcp",
    "build:react": "(cd packages/react && bun run build)",
    "build:mcp": "(cd packages/mcp && bun run build)",
    "lint": "biome check packages/",
    "lint:fix": "biome check --write packages/",
    "format": "biome format --write packages/",
    "clean": "bash clean.sh"
  },
  "workspaces": [
    "packages/**"
  ],
  "devDependencies": {
    "@biomejs/biome": "2.2.3"
  }
}
